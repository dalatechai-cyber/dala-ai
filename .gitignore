node_modules/
.next/
.vercel/
out/
dist/
build/

# Secrets never enter the repository. See CLAUDE.md.
.env
.env.*
!.env.example

*.log
.DS_Store
.idea/
.vscode/
coverage/
*.tsbuildinfo

# Scratch probes. A diagnostic is worked out here and its conclusion belongs in a test —
# `git add -A` swept two of them into a commit on 2026-09-18, which is why this line exists.
.probe-tmp/

Сайн байна уу! Дэлгэрэнгүйг хувийн мессежээр хүргэе. Бидэн рүү мессеж бичээрэй.

# ---------------------------------------------------------------------------
# DRAFT — unsigned, loaded by nothing. The founder rewrites the wording.
#
# Canned-response kind: "comment_public_reply"   (seeded by migration 0007)
#
# This is NOT a gate block. It is the template for a per-tenant `canned_responses`
# row, so the production sign-off is `canned_responses.reviewed_at` per tenant and
# per locale — not `prompt/platform-mn-review.json`, which signs platform text.
# The line above is what Matrix starts from, in their voice, not Dalatech's.
#
# WHAT IT IS FOR
#   The ENTIRE public reply to a comment on the tenant's own Page post. Posted
#   unconditionally, identically, whatever the comment said. There is no code path
#   from the customer's words to this text — `decideCommentReply` is not given the
#   comment's text at all.
#
# WHEN IT FIRES
#   A customer comments on one of the tenant's posts, the channel's
#   `comment_policy` is `public_only`, and the thread has no reply yet.
#
# WHAT IT MUST NEVER CONTAIN
#   - Any numeral. A price, a time, a duration, a phone number. Ш0's whole reason
#     for existing is that a number in public is permanent and screenshot-able, and
#     this line is the one piece of text that is public BY CONSTRUCTION.
#   - Anything that reads as confirming a booking — including a bare «за» or
#     «болно», which Ш3 forbids for exactly this reason.
#   - A staff name, a service name, or any hint of an answer to what was asked.
#   - An apology. Nothing has gone wrong; a customer asked a question in public.
#
# THE FAILURE IT PREVENTS
#   A price quoted under the salon's own post, visible to everyone, forever. That
#   is Ш0, and until comments existed Ш0 could not fire — there was no public
#   surface. This feature is what makes Ш0 reachable for the first time.
#
# THE SECOND FAILURE, NOW CLOSED
#   Five people commenting separately on one post are five threads, so they used
#   to get five identical replies under that post — spam, on the tenant's own
#   wall. Closed by D-021: ONE public reply per post per rolling 24 hours,
#   `tenant_channels.comment_replies_per_post_per_day`, default 1. The per-thread
#   rule still applies inside it.
#
#   So this sentence is now posted AT MOST ONCE under any given post per day, in
#   whichever thread commented first. Write it to be read by everyone on that
#   post, not as a reply to one person.
# ---------------------------------------------------------------------------

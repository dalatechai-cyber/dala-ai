# Leads, demos and tenant #0: 2026-09-25

1. **Demos can't be generated right now.** Opus 5.5 is wired in, but dalatech-app's Vercel `ANTHROPIC_API_KEY` is rejected with 401 "API key is invalid". Test lead #004 failed at the first variant, so **$0 was spent** and there is no cost to report. That is also why Fable never ran. **Needs:** a valid key in dalatech-app Production, then `RELEASE #004` in Telegram (new this morning; a failed demo now alerts you).
2. **Cost per demo on Opus 5.5 (not measured):** at most **~$2.02** (3 variants × 32k output tokens), likely $1–2. The first working lead logs the real figure (`[pipeline] usage … costUsd=`).
3. **What the visitor sees:** «Баярлалаа» appears only once the lead is saved. App: 2.6 s after the click. dalatech.online: 1.3 s. Both passed on a real phone-sized browser.
4. **Real time to a demo:** 24 h, with no margin in either direction: the email is scheduled for exactly 24 h after the lead (`DEMO_DELAY_HOURS=24`), even though generating and deploying take about 15–30 min. The sites promise three different things: dalatech.online «хэдхэн секундэд» (a few seconds), the app form «2 хоногийн дотор» (within 2 days), the app's thank-you «24 цагийн дотор» (within 24 hours). Your call on the wording.
5. **What you receive:** Telegram arrived for all three test leads. The app's email reached dalatech.ai@gmail.com 3 s after the lead, but went to **one recipient only**: `LEAD_NOTIFY_EMAIL` is unset or is the company address, so the app has never emailed your personal inbox. **Needs:** you set it in dalatech-app Production. Vercel refused me (403).
6. **dalatech.online email is not proven to land in both inboxes.** Gmail's SMTP server accepted both addresses (`250 OK`, now logged per recipient). After 12 min, nothing is in dalatech.ai@gmail.com, including spam and trash. The app's email (sent via Resend) lands there in seconds. **Check:** did your personal inbox get the two «TEST — Claude» emails from 07:05 and 07:13 UTC? Does dalatech.ai have a filter?
7. **Abuse protection is live:** 3 leads per IP per day, 20 per day site-wide, a honeypot, and a minimum fill time; suspected bots are held for `RELEASE`. Turnstile switches on when you add its keys.
8. **Tenant #0:** its data is ready for your read, nothing written or republished: [`2026-09-25-tenant0-data.md`](2026-09-25-tenant0-data.md). The live prompt has none of the knowledge base, and a republish changes the Facebook Page's answers too. The doc also has the exact Turnstile and mint-secret steps.
9. Test leads to ignore: app **#004** and site requests `2e48a25d…`, `b8507626…` (all «TEST — Claude», phone 00000000). PRs: dalatech-online [#41](https://github.com/dalatechai-cyber/dalatech-online/pull/41), [#42](https://github.com/dalatechai-cyber/dalatech-online/pull/42); dalatech-app [#2](https://github.com/dalatechai-cyber/dalatech-app/pull/2), [#3](https://github.com/dalatechai-cyber/dalatech-app/pull/3).

# V1 — the cut line

**The problem this document exists to solve:** every item in the architecture is
justified, so nothing is *obviously* cuttable. That is exactly how four months pass with
no customer message flowing and no revenue collected. This file draws the line, in
writing, so that "we should also…" has something to argue against.

Status: **plan, nothing built.** ROADMAP Phase 3.0 item 4.

**Updated 2026-09-01.** This file was written at 16:54 on 2026-08-31; D-009 closed at
19:34 the same day. The contingency it named under *What would make me wrong* — "if the
bake-off says Reception needs a model the ceiling cannot afford" — **fired**, and has since
been **resolved** by D-015 (a 400-conversation band). See
[The ceiling contradiction](#the-ceiling-contradiction-—-resolved-2026-09-01-d-015), which
also corrects an overstatement in how it was first written up.

---

## V1 is done when this sentence is true

> **A real customer messages Matrix Eco Salon's Facebook Page, gets a correct Mongolian
> reply generated from the tenant's own knowledge base and sent on that tenant's own
> token, the cost is recorded against that tenant's ceiling before the call is made, and
> the founder can prove all four from the database.**

Not "the platform works." Four facts, each checkable. Everything below is in V1 if it is
required for that sentence and out if it is not.

Two commercial conditions ride alongside it, because a technically live tenant who
cannot be billed is not a business: the salon is **invoiced and has paid**, and the
Anthropic account has a **provider-side spend limit** set. Neither is code.

---

## What is in

One channel, one tenant, one direction, metered.

| Block | Why it cannot be cut |
|---|---|
| Webhook: raw-body signature verification, fast 200 | Without it Meta unsubscribes the Page under load, and an unverified endpoint is an open relay |
| **Per-entry** tenant resolution from `channel_identity` | The whole product. One POST can carry two tenants |
| The 200/500 asymmetry | An unknown Page must 200 (permanent — a non-200 risks losing the asset); an unreadable registry must 500 (transient — a 200 drops the event forever) |
| Durable queue + worker (QStash) | The ancestor's fast-ACK pattern, which is the one thing it got structurally right |
| `webhook_events` idempotency + lease | Meta retries. Without this a retry is a second bill and a second reply |
| Per-tenant token from `tenant_secrets`, decrypted per request | A module-scope cache is a cross-tenant credential leak on a warm lambda |
| Send to `/{page-id}/messages` | With `/me`, a token/tenant mismatch **succeeds** and posts as the wrong salon. There is no error to catch |
| Config tables + the prompt compiler + `config_snapshots` | The runtime must read a snapshot, not live tables, or a half-finished edit reaches a customer |
| The Ш0–Ш9 boundary gate + the output guard | The failure mode that costs the tenant money and Dalatech its reputation |
| Reserve→settle ledger with a hard ceiling | Reserve **before** the call. A ceiling checked after is not a ceiling |
| The compiled per-surface cap | The one control no query, cache or table can change |
| Telegram alerts | 80%, 100%, token expiry, unknown Page id, ledger unavailable |
| `catalog.sql` + `isolation.sql` in CI | Already written and green. Wiring them to CI is hours |
| Six CI checks | `no-store` guard · no `\b`/`\w`/`[a-z]` over user text · no `process.env.*CEILING|BUDGET|LIMIT` · env-example parity · no bare Supabase client · matcher sanity |

## What is out, and when it comes back

Each of these is deferred with a reason, not forgotten. **None is a maybe.**

| Deferred | Returns | Why it can wait |
|---|---|---|
| **Instagram** | V1.1, after Messenger has been live two weeks | Same seam, more Meta surface, and `entry[].id` semantics for `instagram` are the least-verified thing in the design |
| **Comment replies** | V1.2 | Expands App Review scope and adds rate-budget pressure; a policy strike takes down every tenant at once |
| **The Quality layer** | Phase 5 | At two tenants, the founder reading conversations *is* the Quality layer. It is founder tooling that costs money to run |
| **Analytics automation** | V1.2 | Hand-write the first monthly report. It will teach you what the automated one should say, and the attribution question is unanswered until the booking-site check is done |
| **The probe-token onboarding flow** | Tenant #3 | Bind tenants #1 and #2 by hand. Automating a thing done twice is premature |
| **Volatile/stable prompt split** | With platform-shared caching, Phase 5 | Worth little at two tenants |
| **KEK *rotation* machinery** | When a rotation is actually needed | **Escrow is not deferred** — it is a 30-minute Phase 3.0 item and losing the KEK is unrecoverable |
| **The web widget** | Retire or design it | The one surface where the tenant-identity rule has no answer. It should not be left running either way |
| **Tenant-owner login** | Undecided (item 12) | Policies ship dormant. Telegram is already the channel to this person |
| **Retrieval / embeddings** | Per-tenant trigger | `0002` exists; `app.search_kb()` ships in `0001` so nobody hand-rolls a vector query outside a tenant scope |
| **Billing automation** | After tenant #2 | For tenant #1 a bank transfer and a `paid_through` date is a fine answer — **say so rather than leaving it undesigned** |

---

## The ceiling contradiction — RESOLVED 2026-09-01 (D-015)

**Resolved by the founder: Reception is sold against a band of 400 conversations/month.**
Kept here rather than deleted, because how it was found and how it was mis-stated are both
worth keeping.

### What it was

Two settled decisions were each correct alone and collided when combined:

| | |
|---|---|
| **D-004** derives the ceiling from the *discounted floor* | ₮200,000/mo at a 60% target margin → **$22.86/month** allowable model spend |
| **D-009** chose the model by measurement and native-speaker review | Sonnet 5, **~$0.0090 per reply** blended |

At the design's own A7 (6 messages/conversation), $22.86 buys **2,540 replies ≈ 423
conversations**. Reception was being sold with no stated limit against a ceiling that
affords 423.

### The resolution

The ceiling does not move and D-004's formula is untouched. **What was sold moves**: 400
conversations/month included, with ~5% headroom under the 423 the ceiling actually buys.
Overage is the existing degradation ladder, not an invoice — billing overage is undesigned
and must stay that way while no revenue path exists. Full reasoning in
[`DECISIONS.md` D-015](DECISIONS.md).

### Correction: I overstated what happens at the ceiling

An earlier revision of this section said Reception *"stops replying to Matrix's customers
around day 17 of every month. Not degrades: stops."* **That was wrong**, and wrong in the
direction of alarm. [`05-spend-ledger.md` §5.7](architecture/05-spend-ledger.md) designs a
five-state ladder, and state 2 keeps the salon meaningfully alive: deterministic shortcuts
still answer location, hours and greetings, canned refusals still answer, the closure
message still answers, and anything needing the model gets the Mongolian handoff line to
7741-7777 **once per conversation**.

So the real failure is *"for part of the month the AI receptionist becomes an address-and-
hours bot that tells customers to phone"* — a genuine product problem and not what a salon
paid for, but not silence. I asserted the stronger claim without reading §5.7, which is the
same mistake as the composition-table misses: **a claim about a document, made without
reading the document.**

### And the overshoot rests on a number nobody has measured

The 1.77× that forced this decision assumed **750 conversations/month** — a guess, and one
the design does not agree with itself about. `05-spend-ledger.md` assumes **300–600 at ~5
replies each**:

| Real volume | Monthly spend | vs the $22.86 ceiling |
|---:|---:|---|
| 300 conv | $16.20 | 0.71× — comfortably under |
| 420 conv | $22.68 | 0.99× — exactly at |
| 600 conv | $32.40 | 1.42× — over |
| 750 conv | $40.50 | 1.77× — over |

### Measured 2026-09-01 — Matrix was already inside the band (D-016)

Six days of the ancestor's production logs: mean **60.5 replies/day → ~1,842/month, 41%
of the assumed 4,500.** Spend **$16.57/mo, 0.73× the ceiling, 71% margin** — clearing the
60% target. A8 = 750 is refuted; `05-spend-ledger.md`'s 300–600 was closer.

**The margin result is solid** (it depends only on the measured reply count). **The
conversation count is not** — it still divides by the unmeasured A7 = 6. Count distinct
PSIDs instead; the log line carries them.

The band stays at 400: the busiest day measured (94), sustained, is ~477 conversations —
outside it. Six days carries weekly shape and no seasonal shape, so the mirror phase's 14
days remains the measurement that sets the final number.

---

## The build order

Dependency-ordered and PR-sized. Each has a done-test that can fail. Estimates assume
one person and are the thing most likely to be wrong.

### Track 0 — before any code (days, not weeks)

| # | Work | Done when |
|---|---|---|
| 0.1 | Provider spend limit at Anthropic; KEK generated and escrowed; second admin on Meta, Supabase, GitHub, registrar | A written break-glass note names where each lives |
| 0.2 | Supabase project created; `0001` applied via CLI | `catalog.sql` 18/18 **against the real project**, pasted |
| ~~0.3~~ | ~~`count_tokens` on the real prefix; bake-off arms D vs E~~ **DONE 2026-08-31** | D-009 closed on Sonnet 5. The ceiling was **not** recomputed — recomputing it is what surfaced the contradiction above, and the number is the founder's to choose |
| 0.4 | Meta app, Business Verification, permission requests submitted | Submitted — this is the ~20-day long pole and it starts on day 1 |

**0.4 gates the cutover and nothing else, so it runs in parallel with everything below.**

### Track 1 — the spine (week 1–2)

| # | Work | Done when |
|---|---|---|
| ~~1.1~~ **DONE** | Next.js 16 (D-018), shared clients with `cache: 'no-store'`, plus FOUR guards: no-store, env parity, ceilings-not-env, Cyrillic matchers | ✅ The no-store guard fails in both directions; all four run in CI |
| ~~1.2~~ **DONE** | `GET+POST /api/webhooks/meta/[app]`: raw **bytes** (`arrayBuffer`, not `text`), 1 MB cap, `X-Hub-Signature-256` over a SET of app secrets, fast 200 | ✅ 8 tests: forged/tampered/malformed all 401, Cyrillic bodies verify, absent secret refuses |
| ~~1.3~~ **DONE** | Per-entry resolution + the 200/500 asymmetry + the app-vs-identity cross-check | ✅ 4 tests pin the asymmetry in both directions; unknown Page persists tenant-less and 200s, dead registry 500s |
| ~~1.4~~ **DONE** | `webhook_events` claim-before-work, `unique (provider, dedup_key)` **global** | ✅ 5 tests; one pins that the dedup key carries no tenant_id |
| ~~1.5~~ **WRITTEN, not yet proven** | QStash enqueue with `deduplicationId`; worker verifies the signature against BOTH signing keys | ⚠️ Unit-tested only. The redelivery/crash property needs a live QStash and is **not** verified |

**Milestone: inbound is safe.** No model has been called yet and no money spent.

### Track 2 — the money, before the first model call (week 2)

| # | Work | Done when |
|---|---|---|
| ~~2.1~~ **DONE** | `withTenantRole`: identity → entitlement → consent → budget, each failing closed | ✅ 13 tests. Each step proved to 503 on an unreadable table rather than pass; a platform-GATED role refuses even when the tenant row says active; entitlement is checked before budget so a refusal reserves nothing |
| ~~2.2~~ **DONE, one half unprovable** | `reserve_spend`/`settle_spend` wired; ceilings compiled in `config/platform.ts`, which `tenant_budgets` can only ever LOWER | ✅ A tenant at its cap is refused before an Anthropic client is constructed — pinned by a named test. ⚠️ `isolation.sql` T8/T9 **against the real project** is NOT done and cannot be: no Supabase project exists. They pass against scratch Postgres in CI |
| ~~2.3~~ **DONE** | Telegram alerts, deduplicated in **Postgres** (not Redis — a dedup store that fails open is not a dedup store) | ✅ 500 occurrences of one condition produce exactly ONE alert row and 499 suppressions. The period is in the dedup key, so the same ceiling tomorrow is a new alert |

**This lands before Track 3 on purpose.** The first model call the system ever makes
should already be metered — and now the meter exists before the thing it meters, so there
is no window in which the code can spend before it can count. The worker runs the full
chokepoint and takes a reservation today; it releases it immediately, because there is
nothing yet to spend it on.

### Track 3 — the reply (week 3–4)

| # | Work | Done when | Status |
|---|---|---|---|
| 3.0 | The Mongolian text engine: stem-prefix matching, ordered stem sequences, script share, code-point lengths, numeral and link extraction | A checked-in fixture of inflected forms matches offline | **done** |
| 3.1 | Prompt compiler: config tables → `config_snapshots`; publish + rollback | Publishing is one insert and a pointer move; rollback is one UPDATE | **done, unseeded** |
| 3.2 | Reception call on **`claude-sonnet-5`** (D-009), `prompt_cache_mode='1h'`, thinking pinned disabled, `max_tokens: 700`, zero tools | `usage` is logged on every call, including `stop_reason` | **done, unwired** |
| 3.3 | Ш0–Ш9 boundary gate rendered from `disclosure_rules` / `out_of_scope_topics` | The children's-haircut probe refuses to quote and names no number | |
| 3.4 | Output guard: every numeral in the reply appears in `allowed_numbers` | An injected fake price fails the guard and falls back to the canned refusal | **done, uncalled** |
| 3.5 | Send via `/{page-id}/messages`; `outbound_messages` claim + lease | A redelivery re-sends the **stored** text, never re-generates | **claim + lease done; the send is founder-gated** |

**Milestone: the sentence at the top is true in staging.**

#### 3.0 was not in the original plan, and it should have been

§6.7(b) calls an offline fixture of inflected forms "the cheapest correctness win in the
whole design and should be built in week one", and then the plan had no row for it. It is
now 3.0 because 3.3, 3.4 and the deterministic short-circuit all match Mongolian text and
would otherwise each have grown their own matcher.

The engine exists because the constructs one reaches for first are not merely imprecise
here — they are silently, systematically wrong. Verified by execution on this repository's
Node, and pinned as tests so they stay verified:

| | On Mongolian |
|---|---|
| `/\bзасалт\b/` | `false` |
| `/\w/` on `үс` | `false` |
| `'😊'.length` | `2`, so a length cap truncates mid-emoji |
| `Number('᠓')` | `NaN`, so a Mongolian-script digit reads as "not a number" |

That last one was found while writing 3.4 and is a hole an injected price could have used:
a numeral guard written the obvious way sees `᠓᠓,᠐᠐᠐` as containing no number at all.

The same execution settled why `unaccent` is *partially* destructive rather than merely
lossy, which CLAUDE.md rule 6 asserts without the mechanism: `Ё` and `Й` decompose under
NFD and `Ө` and `Ү` do not, so stripping combining marks flattens the two letters Russian
shares and leaves untouched the two that make the text Mongolian. It therefore works on
nine strings in ten.

**3.4 is done but uncalled.** The guard is complete and tested against 25 cases; nothing
invokes it yet, because the thing it guards — the model call — is 3.2. It is written first
on the same principle as Track 2: the reply is metered before it exists, and now it is
checked before it exists too.

#### 3.1 is done and deliberately unseeded

The compiler, the publish/rollback pointer moves, the review gate and the KB write-time
check are built. **`prompt/platform/` is empty**, so no platform block exists to compile
yet, and the gate refuses rather than rendering nothing.

That split is the point. Reviewing Mongolian prose and reviewing a build gate are
different jobs for different people, so the machinery merges on its own and the text
arrives as a change whose entire diff is text a native speaker can read. §6.6 asks for
exactly this and the draft did not have it: the `reviewed_at` gate covered *tenant* rows
while the platform's own Mongolian was covered only by code review, by people who do not
read Mongolian — and three errors had already survived into the draft's platform text,
the worst of them a duplicated verb in the one sentence whose job is to stop the model
following instructions pasted into a knowledge base.

`scripts/guards/check-mn-review.mjs` signs each block by **file hash**, so editing a block
after sign-off invalidates it. Proven to fail in all four directions before it was
committed — unsigned block, correct signature, edited-after-signing, and a stale signature
for a file that no longer exists — because a guard that has only ever been seen to pass is
not known to be a guard.

**Rollback re-renders nothing**, and that is a property rather than an optimisation: the
previous snapshot is still there byte-for-byte with the same `content_hash`, so the
prompt-cache entry it warmed is still valid. A rollback that recompiled could produce a
*different* prefix from the same rows if any input had drifted, which is the one thing a
rollback must never do.

#### Open, and it needs a decision before 3.2 ships

**May a reply repeat a numeral the customer wrote?** `allowed_numbers` is compiled from the
prompt, so a customer who asks «Маргааш 15:00 цагт болох уу?» gets a reply mentioning
15:00 refused, and the pinned handoff line instead.

Loosening it is not obviously right. «Үс засалт 5000₮ юу?» puts a number in front of the
model that the salon never set, and echoing it back as confirmation invents a price — the
customer merely suggested it. So the two cases pull opposite ways: a time the customer
proposed is theirs to state, a price they proposed is not.

The conservative reading ships today, because a false refusal costs a handoff line and a
false pass costs a price the salon is expected to honour. It is the §6.5 composition trade
again and it is the founder's call, not mine. A middle option exists — allow echoed
numerals that are **not** currency-shaped — and it is cheap to build once someone decides
it is wanted.

#### 3.2 is done and deliberately unwired

The request builder, the failure classifier, the timeout and the stale-event check are
built and tested. **Nothing calls them.** The worker still releases its reservation
without spending, exactly as Track 2 left it, and it stays that way until the platform
blocks exist — because a compiler with no blocks has no prompt to send.

Three request properties are not stylistic, and each was checked against the current API
contract rather than written from memory:

- **`thinking: { type: 'disabled' }`.** Sonnet 5 accepts it and **rejects `budget_tokens`
  with a 400**, so pinning thinking off is the only way to stop it eating the reply budget
  (D-014). There is no "just cap the thinking" option to fall back on.
- **`tools` is absent, not an empty array.** The single largest reduction in what a
  successful injection can achieve: there is no tool to call.
- **No `temperature`, `top_p` or `top_k`.** These are *removed* on Sonnet 5 and return a
  400. Porting the ancestor's `temperature` would have failed every request — a check
  worth making before the first call rather than after it.

**`stop_reason` is read before `content`.** A safety refusal arrives as HTTP 200, so code
that reaches for `content[0].text` first sees a plausible body and mislabels a terminal
condition as a normal reply. That is precisely how the ancestor turns one refusal into
four paid calls.

##### A pre-existing §6.2.6 violation, found by building the guard

§6.10.5 asks for a CI grep for date-suffixed model ids. Writing it surfaced something
else: `scripts/bakeoff/run.mjs` carried its own copies of both model ids and both price
tables. Two registries, in two languages, free to diverge — which is the duplication
§6.2.6 forbids, spelled differently enough that nobody noticed.

Fixed by making `config/models.json` the one registry that both the TypeScript app and
the `.mjs` harness read. Exempting the harness would have been easier and would have left
the second copy in place.

The database already had half of this check — `catalog.sql` V12 asserts no date-suffixed
id in `model_prices`. The new guard covers the source, which V12 cannot see.

#### 3.5, the half that needs nobody's permission

The claim and lease are built; **the send is not**, because it needs a decrypted per-tenant
Meta token and no KEK exists.

The done-test is the whole reason this half exists. QStash guarantees **at-least-once**
delivery of the job; Messenger needs **at-most-once** delivery of the reply. Those
contradict each other unless something turns "run this job again" into "finish sending the
message we already wrote" — which is what the draft row does. Without it a redelivery
re-enters generation: a second paid Anthropic call, a second pass through the boundary gate
that may answer differently, and two different replies to one customer question.

Uniqueness is the partial index `(tenant_id, kind, dedup_key)`, not a check-then-insert:
two workers racing on the same redelivery both attempt the insert, one wins on `23505`, and
the loser reads the winner's row. A read-then-decide-then-write lets both through. The
claim is likewise one conditional UPDATE with the state CAS and the lease check **inside
the WHERE clause**.

**`indeterminate` is a seventh state and it earns its place.** A send that timed out
*after the request left* may have been delivered. Retrying replies to the customer twice;
marking it failed loses a reply that may never have arrived. So it is neither — it is
parked, `indeterminate` is not claimable, and no automatic retry can pick it up. The
ancestor already learned this one level up (`messenger.js:118-133` deliberately does not
retry a timed-out enqueue while it does retry a hard-failed one), and it is exactly the
distinction a rewrite loses.

### Track 4 — tenant #1 (week 5–6)

Phases C, E, F, G of the onboarding checklist, then D (mirror), H (shadow), I (cutover),
J (handover). The mirror phase is what makes the cutover cheap: 14 days of real inbound
timestamps, zero risk, no subscription change — and it is also what sets
`prompt_cache_mode` from measurement instead of a guess.

---

## Which onboarding steps V1 skips

The checklist is ~41 numbered steps across ten phases. V1 executes all of Phases A, C,
D, E, F, G, H, I, J. It **skips or defers** these:

| Step area | V1 |
|---|---|
| Instagram channel binding, IG-Login rehearsal | **Skipped.** `auth_flavour='instagram_login'` stays out of the constraint until a step rehearses it |
| Comment-field subscription and enrichment | **Skipped.** DM-only, which also narrows App Review |
| Quality-review provisioning (`quality_review` capability, corpus) | **Deferred.** `tenant_roles` row exists at `off` |
| Analytics provisioning (`tenant_domains`, redirect key, baseline capture) | **Partly.** Register `tenant_domains` and mint links — clicks are cheap and the 3-month baseline can only be captured *before* launch, so it is not deferrable. The monthly job is |
| Probe-token proof-of-possession flow | **Manual.** A founder-run probe still satisfies `active_requires_probe_run`; the automated flow waits for #3 |
| `handoff_targets` + Telegram `/start` with the salon | **Not skipped.** Without it the handoff path has no destination |
| Consent bootstrap + opt-out phrase list | **Not skipped.** Cheap, and it is the gate the other sections forget |
| Retention scheduling (`ops.purge_expired`) | **Not skipped.** A retention promise you cannot honour is worse than none |

**Two steps are load-bearing and easy to skip under deadline pressure, and both are
enforced by CHECK constraints rather than discipline:** a tenant cannot go `active`
without a published config, and cannot go `active` without a passing probe run. If the
cutover step fails, that is the constraint working.

---

## What would make me wrong

- **The 4–6 week estimate.** It assumes Meta App Review clears in ~20 days and that the
  Business-Portfolio token dance works first time. Neither is verified, and the research
  behind both is marked SEARCH-CORROBORATED at best because Meta's docs were unreachable.
- ~~**Track 3 assumes D-009 is settled.** If the bake-off says Reception needs a model the
  ceiling cannot afford, the plan pauses on a pricing decision, not an engineering one.~~
  **This happened, and is now closed.** The bake-off said exactly that; the founder
  resolved it with a conversation band (D-015). Recorded here rather than edited away,
  because a risk register is only worth keeping if it is scored afterwards — and this one
  scored: the risk was correctly identified in advance, and the response took one decision
  rather than a redesign.
- **Analytics being "deferred" assumes the booking-site query-parameter check comes back
  positive.** If it does not, Matrix is capped at Tier A + C forever and the first report
  is a different, smaller document.
- **The mirror phase assumes 14 days of lead time.** If the salon wants the bot switched
  next week, `prompt_cache_mode` ships on a guess and the cost model degrades.

---

## The rule this document is defending

Anything not in the table above does not go into V1 **even when it is a good idea**,
and especially when it is a good idea. The measure is the sentence at the top: a real
customer, a correct Mongolian reply, on the tenant's own token, metered before the call.
Everything else is V1.1.
